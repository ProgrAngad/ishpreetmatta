# Portfolio, Ishpreet Kaur Matta

A static one-page portfolio. Plain HTML and CSS, no build step, no JavaScript, and no
external requests.

- `index.html` and `styles.css`: the page
- `assets/`: one screenshot
- `files/`: an Excel allocation tracker, its one-page summary, and a resume

The allocation tracker runs on sample data. Not from any employer.
